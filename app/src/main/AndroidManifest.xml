<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sayem.resturantdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".FastFoodList" />
        <activity android:name=".BurgerActivity" />
        <activity android:name=".PizzaActivity" />
        <activity android:name=".SubwayActivity" />
        <activity android:name=".LaunchList" />
        <activity android:name=".DrinksActivity" />
        <activity android:name=".CofeeActivity" />
        <activity android:name=".LemonadeActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".SplashScreen" />
        <activity android:name=".FnfList" />
        <activity android:name=".OrderActivity1"></activity>
    </application>

</manifest>